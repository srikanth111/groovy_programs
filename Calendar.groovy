import org.codehaus.groovy.runtime.InvokerHelper
import static Calendar.getInstance as now
class Calendar1{
	 static void main(String[] args)
	 {
	 	//println '$now'
	 	assert now().Calendar1 == Calendar.getInstance().Calendar1
	 }
}